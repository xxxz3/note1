在 Python 中，hasattr() 是一个非常实用的内置函数，全称是 "has attribute"。它的核心作用是：检查一个对象（Object）是否拥有特定的属性或方法

在 JumpServer 使用的 Django Rest Framework (DRF) 框架中，initial_data 是一个非常关键的“原材料”仓库。
简单来说，initial_data 存放的是用户通过 API 传过来的“原始、未经清洗”的数据。

在 Django Rest Framework (DRF) 的上下文（以及 JumpServer 的源码）中，instance 指的是“数据库中已存在的对象实例”。
简单来说，instance 代表的是**“旧数据”，而 initial_data 代表的是用户传过来的“新数据”**。

@property (属性化)
它让你可以像访问变量一样访问函数，非常适合包装逻辑。
class User:
    @property
    def full_name(self):
        return f"{self.first_name} {self.last_name}"
# 调用：user.full_name  (不需要 user.full_name())

@classmethod (类方法)
当你需要操作“类本身”而不是某个具体的“对象”时使用。
class App:
    version = "1.0"
    
    @classmethod
    def get_info(cls):
        return f"App version: {cls.version}"

@staticmethod (静态方法)
它其实就是一个放在类里面的普通函数，它不需要 self 也不需要 cls。
class Utils:
    @staticmethod
    def is_valid_email(email):
        return "@" in email

